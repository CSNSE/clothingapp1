type Shirt @model @auth(rules: [{allow: public}]) {
  id: ID!
  Type: String!
  Size: String!
  Brand: String!
  Image: String!
}

type Note @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  image: String
}
 